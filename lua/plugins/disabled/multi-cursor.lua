-- NOTE: inconvenient, takes up a lot of important buttons (
-- <C-Up/Down> - mission control in mac
-- <S-Left/Right> - fast move between words (in my case (e/b in common case))
-- ),
-- but all actions can be performed using standard Vim functions
return {
   'mg979/vim-visual-multi',
   branch = 'master',
}
